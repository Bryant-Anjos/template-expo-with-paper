image: node:14

variables:
  DOCS_FOLDER: 'docs'

cache:
  paths:
    - ./node_modules

pages:
  stage: deploy
  before_script:
    - yarn install
    - yarn build-storybook
  script:
    - mkdir public
    - cp -rp $DOCS_FOLDER/* public
  artifacts:
    paths:
      - public
  only:
    - main
