image: node:14

cache:
  paths:
    - ./node_modules

pages:
  stage: deploy
  before_script:
    - yarn install
  script:
    - yarn build-storybook
  artifacts:
    paths:
      - public
      - src/components/**/*
      - src/screens/**/*
      - stories/**/*
  only:
    - main
